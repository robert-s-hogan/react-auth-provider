import React from "react";
import DisplayAuthenticated from "./DisplayAuthenticated";

const HomePage = () => {
  return (
    <div>
      <DisplayAuthenticated />
    </div>
  );
};

export default HomePage;
